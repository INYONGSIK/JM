<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ucamp.JM.dao.MyMusicDAO">

    <select id="selectMyMusic" resultType="MyMusic">
        select MyMusic_title, MyMusic_singer, MyMusic_genre,
        to_char(to_date(MyMusic_release,'YYYY/MM/DD')) as "MyMusic_release"
    </select>

</mapper>
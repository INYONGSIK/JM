<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ucamp.JM.dao.MusicDAO">

    <select id="selectMusic" resultType="Music">
        <![CDATA[select music_title, music_singer, music_genre,to_char(to_date(music_release,'YYYY/MM/DD')) as "music_release",
    music_like from music where rownum <= 10]]>
    </select>

    <insert id="insertTodayMusic" parameterType="Music">
        insert into today_music(rank_number,rank_date,music_title,music_singer,music_genre,music_like)
        values(seq_today_music.NEXTVAL,SYSDATE,#{music_title},#{music_singer},#{music_genre},#{music_like})
    </insert>

</mapper>





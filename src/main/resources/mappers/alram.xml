<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ucamp.JM.dao.AlarmDAO">

    <select id="selectFollower" parameterType="int" resultType="User">
        select * from jmuser where user_number in (select follower from follow where followee = #{followee})
    </select>
    <select id="selectFollowee" resultType="User">
        select * from jmuser where user_number in (select followee from follow where follower = #{follower})
    </select>
    <select id="deleteFollow">
        delete follow where follower=#{follower} and followee = #{followee}
    </select>
    <insert id="insertFollow">
        insert into follow values (follow_seq.nextval,#{follower},#{followee})
    </insert>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ucamp.JM.dao.PlaylistDAO">

    <select id="selectPlaylist" resultType="Playlist" parameterType="Map">
        select music_number from playlist where list_name = '${list_name}' AND user_number = ${user_number}
    </select>


    <insert id="insertPlaylist" parameterType="Playlist">
        <selectKey keyProperty="playlist_cd" resultType="int" order="BEFORE">
            SELECT playlist_num.NEXTVAL FROM DUAL
        </selectKey>
        insert into playlist
        values (playlist_num.NEXTVAL, #{music_number}, #{list_name}, #{user_number})
    </insert>

    <select id="PgetUserNumByEmail" resultType="User">
        select user_number from jmuser where user_email= #{user_email}
    </select>

    <select id="PgetListNameByUserNum" resultType="String" >
        select list_name from playlist_manage where user_number = #{user_number}
    </select>

    <select id="selectAllP" resultType="playlist_manage">
        select * from playlist_manage where user_number='${user_number}'
    </select>

    <select id="selectByMusicNumber" resultType="Music">
        select * from music where music_number = #{music_number}
    </select>

</mapper>
